{{- $svc := index .Values "db-manager" }}
apiVersion: v1
kind: Service
metadata:
  name: db-manager
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "modu-erp.labels" . | nindent 4 }}
    app.kubernetes.io/name: db-manager
spec:
  type: {{ $svc.service.type }}
  selector:
    app.kubernetes.io/name: db-manager
  ports:
    - port: {{ $svc.service.port }}
      targetPort: {{ $svc.service.port }}
      protocol: TCP
