{{- $svc := index .Values "erp-organization" }}
apiVersion: v1
kind: Service
metadata:
  name: erp-organization
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "modu-erp.labels" . | nindent 4 }}
    app.kubernetes.io/name: erp-organization
spec:
  type: {{ $svc.service.type }}
  selector:
    app.kubernetes.io/name: erp-organization
  ports:
    - port: {{ $svc.service.port }}
      targetPort: {{ $svc.service.port }}
      protocol: TCP
