{{- $svc := index .Values "kerberos" }}
apiVersion: v1
kind: Service
metadata:
  name: kerberos
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "modu-erp.labels" . | nindent 4 }}
    app.kubernetes.io/name: kerberos
spec:
  type: {{ $svc.service.type }}
  selector:
    app.kubernetes.io/name: kerberos
  ports:
    - port: {{ $svc.service.port }}
      targetPort: {{ $svc.service.port }}
      protocol: TCP
