{{- $svc := index .Values "vacation" }}
apiVersion: v1
kind: Service
metadata:
  name: vacation
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "modu-erp.labels" . | nindent 4 }}
    app.kubernetes.io/name: vacation
spec:
  type: {{ $svc.service.type }}
  selector:
    app.kubernetes.io/name: vacation
  ports:
    - port: {{ $svc.service.port }}
      targetPort: {{ $svc.service.port }}
      protocol: TCP
